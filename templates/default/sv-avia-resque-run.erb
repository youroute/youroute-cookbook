#!/bin/zsh
exec 2>&1
cd <%= @options[:rails_root] %>
exec chpst bundle exec rake workers:start COUNT=<%= @options[:workers_count] || 8 %> RAILS_ENV=<%= @options[:rails_env] || "development" %> VERBOSE=<%= @options[:verbose] || 0 %> >> log/resque_workers.log 2>&1